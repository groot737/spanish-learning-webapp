<!DOCTYPE html>
<html lang="ka">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ესპანურის სიტყვები🇪🇸</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="top-button">
    <button class="bg-gray-700 text-white text-base font-medium py-2 px-6 rounded-md shadow-md hover:bg-gray-600 transition duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50" id="openModalButton">
      სიტყვის დამატება
    </button>
  </div>

  <!-- Content Container -->
  <div class="content-container flex flex-col items-center">
    <h1 class="text-3xl sm:text-4xl font-bold text-center text-white-400 mb-8">
      ესპანურის სიტყვები 🇪🇸
    </h1>

    <!-- Table Container -->
    <div class="w-full bg-gray-800 rounded-lg shadow-lg">
      <table class="w-full border border-gray-700">
        <thead class="bg-gray-700 border-b-2 border-gray-600">
          <tr>
            <th class="px-6 py-3 text-center text-lg font-bold text-gray-200 uppercase tracking-wider">სიტყვა</th>
            <th class="px-6 py-3 text-center text-lg font-bold text-gray-200 uppercase tracking-wider">განმარტება</th>
          </tr>
        </thead>
        <tbody class="bg-gray-800 divide-y divide-gray-700">
          <% words.forEach(word => { %>
            <tr>
              <td class="px-6 py-4 text-center whitespace-normal text-sm font-medium text-gray-100"><%= word.word %></td>
              <td class="px-6 py-4 text-center whitespace-normal text-sm text-gray-300"><%= word.definition %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Modal (Popup) for Adding Word -->
  <div id="modal" class="modal">
    <div class="modal-content">
      <h2 class="text-2xl font-bold text-white mb-4">დაამატე სიტყვა</h2>
      <form id="addWordForm" action="/add" method="POST">
        <div class="mb-4">
          <label for="word" class="block text-sm font-medium text-gray-200">სიტყვა</label>
          <input type="text" id="word" class="w-full mt-2 px-4 py-2 bg-gray-700 text-white rounded-md" placeholder="დაამატე სიტყვა" name="word" required>
        </div>
        <div class="mb-4">
          <label for="definition" class="block text-sm font-medium text-gray-200">განმარტება</label>
          <input type="text" id="definition" class="w-full mt-2 px-4 py-2 bg-gray-700 text-white rounded-md" placeholder="განმარტება" name="definition" required>
        </div>
        <div class="flex justify-end">
          <button type="submit" class="upload-btn">დამატება</button>
          <button type="button" id="closeModalButton" class="delete-btn ml-4">დახურვა</button>
        </div>
      </form>
    </div>
  </div>
</body>
<script src="script.js" type="module"></script>
</html>
